import { ScrollView, View,} from 'react-native';

const Agregar = () => {
    return (
        <ScrollView>
            <View>
                
            </View>
        </ScrollView>
    )
}
export default Agregar;